## Persistent agent learning

When a non-obvious failure is resolved, a workflow succeeds repeatedly, or the same class of mistake recurs, use the `wikiskill-evolution` skill to decide whether the experience should become durable wiki knowledge or an evolved reusable skill.

Normal task-solving should rely on active skills and repository context, not `.wikiskill/wiki` as a hidden answer source. The wiki is for maintaining and evolving skills.
